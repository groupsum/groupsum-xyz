[project]
name = "groupsum-catalog-api"
version = "0.1.0"
description = "Durable Tigrbl-backed catalog and page-model API for groupsum.xyz"
requires-python = ">=3.12,<3.13"
dependencies = [
  "tigrbl==0.4.5.dev4",
  "tigrbl-engine-duckdb==0.4.5.dev4",
  "tigrbl-engine-postgres==0.4.5.dev4",
  "tigrbl-engine-sqlite==0.4.5.dev4",
  "duckdb>=1.3,<2",
  "orjson>=3.10,<4",
  "pydantic>=2.11,<3",
  "psycopg[binary]>=3.2,<4",
  "uvicorn>=0.35,<1",
]

[dependency-groups]
dev = [
  "httpx>=0.28,<1",
  "pytest>=8.4,<9",
  "ruff>=0.12,<1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/groupsum_catalog_api"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
